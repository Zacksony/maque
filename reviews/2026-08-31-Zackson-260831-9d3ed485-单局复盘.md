# 260831-9d3ed485 单局复盘

## 对局信息与判断口径

- 对局编号：`260831-9d3ed485-63a6-4101-9cd1-8883298016c9`
- 时间：2026-08-31 20:19 至 20:40（Asia/Shanghai）
- 规则：雀魂四人南，标准规则
- 复盘对象：座位 3，Zackson
- 终局：**39500 点，一位，段位分 +70**
- 对局只进行到南一：座位 2 在南一自摸支付后降至 `-500`，被飞并提前结束。

所有牌效、安全度与攻守判断均只使用决策当时已经公开的信息。对手终局暗牌只用于核对实际结果；有效牌枚数按当时可见牌估算，被鸣走的舍牌只计一枚。数牌同种四枚均已计入可见范围，不等于该数牌本身安全；字牌的绝对安全还要排除国士无双例外。

## 总结

本场共有 6 个小局，Zackson：

- 和牌 2 次，均为立直自摸满贯；
- 放铳 0 次；
- 立直 2 次，两次均和牌；
- 副露 1 次；
- 流局未听 1 次。

结果很好，防守执行也明显好于上一场：东二面对庄家立直、随后双立直时彻底弃和，东四面对立直也连续使用主要威胁家的现物；支付不听罚符没有问题。

过程仍有三类会影响长期成绩的问题：

1. 无人攻击时，几次没有先确认打后向听数，拆掉对子或复合形，同时保留孤张、近乎断掉的字牌。最严重的是东二一本场序列 36 与东四序列 24。
2. 四家牌河中的字牌枚数没有持续更新。东三把已经四枚全部计入可见范围的 `4z` 留了多个巡目；东一、东四和南一也有活役牌与近死字牌顺序颠倒。
3. 开放手仍需区分“形状听牌”和“有役可和牌”。东三吃 `6p` 后名义听 `4m/7m`，但起初只有 `7m` 一侧有三色役；后续切 `9m` 反而把合法和牌枚数由 2 枚改善到 4 枚，不能只看名义等待种类。

## 点数进程

| 小局 | 结果 | Zackson 净变化 | 局后点数 | 主要判断 |
| --- | --- | ---: | ---: | --- |
| 东一 | 立直自摸 `4s`，5 番 20 符，2000/4000 | +8000 | 33000 | 立直正确；此前有一处中等牌效损失和字牌排序问题 |
| 东二 | 两家听牌流局，Zackson 未听 | -1500 | 31500 | 对庄家立直及后续双立直完整弃和，正确 |
| 东二一本场 | 座位 0 荣和座位 1 的 `4m` | 0 | 31500 | 序列 36 为本场最大纯牌效错误之一 |
| 东三 | 座位 1 荣和座位 2 的 `2z` | 0 | 31500 | 长期保留死 `4z`；吃牌是高方差边界选择，后续改良正确 |
| 东四 | 座位 0 立直荣和座位 2 的 `3m` | 0 | 31500 | 序列 24 拆雀头退向听；立直后弃和正确 |
| 南一 | 立直自摸 `2s`，5 番 20 符，2000/4000 | +8000 | 39500 | 立直正确；此前有两处中小牌效损失 |

## 逐局复盘

### 东一：立直正确，但幸运进张修复了前一手牌效错误

#### 字牌顺序：应先处理已经更少的牌

序列 14 摸 `8m` 时，座位 2 已经切过一枚 `7z`。实战切自风北 `4z`；两者同为役牌种子，但 `7z` 已少一枚，切 `7z` 略宽，约为 50 枚对 49 枚。这只是极小损失。

序列 22 摸 `5z` 时，座位 1、座位 2 已各切一枚 `6z`，加手中 `6z` 共三枚计入可见范围，未来只剩一枚；`5z` 当时尚有三枚未见。实战摸切更活的 `5z`，应先切近乎断掉的 `6z`，约为 49 枚对 47 枚。序列 30 仍先切 `7z` 而留 `6z`，又是同一类小问题。

#### 序列 46：切 `1m` 比切 `8m` 少约 4 枚有效牌

当时为：

```text
112345899m 23456s
```

实战切一枚 `1m`，打后仍为一向听，约 16 枚有效牌。切孤立度更高的 `8m` 同为一向听，约有：

```text
1m×2、3m×2、6m×4、9m×2、1s×3、4s×3、7s×4
共20枚
```

场上没有现实威胁，这是明确的中等牌效错误。下一巡实际摸到 `6m`，使两条路线重新汇合，所以最终仍和牌；后来和牌不能反证序列 46 正确。

序列 54 切 `8m` 立直后为：

```text
123456m 99m 23456s
听 1s / 4s / 7s
```

按当时可见信息约剩 `1s×3、4s×3、7s×4`，共 10 枚，且没有自家舍牌振听。门清三面立直是标准选择。之后在无人鸣牌的情况下直接自摸 `4s`，成立立直、一发、门前清自摸、平和、里宝牌 1，共 5 番满贯。

### 东二：纯牌效有两处损失，面对双立直的防守正确

宝牌指示牌为 `4z`，因此手中的对白 `55z` 同时是役牌种子和两张宝牌。

#### 立直以前

- 序列 44 摸到第二张 `7s` 后又切一张 `7s`，约为二向听 20 枚；切单张场风东 `1z` 可约 22 枚。已经有对白和两张宝牌，继续保留额外单张东的价值不足以补偿拆对子。这是一处小错。
- 序列 82 摸 `6p` 后切 `2s`，打后为二向听约 17 枚；切 `3p` 或一张 `9p` 可约 21 枚。此时尚无人攻击，是本局主要的中等牌效错误。

#### 序列 86 以后：接受不听罚符是正确防守

座位 1 以 `3m` 立直时，Zackson 仍为二向听，虽然有对白、宝牌 2，但自己领先且对方为庄家，应转为弃和。

- 序列 91 切 `7p`：座位 2 刚切过，是其本人现物；座位 1 已立直并放过，之后同牌持续对其安全；座位 0 本人河中也有 `7p`。该时点可以覆盖三家。数字上由二向听退到三向听不能作为批评理由。
- 序列 95，立直者切出 `5z`，手中对白可以碰成白 1 番＋宝牌 3 的高值开放手。但鸣后仍约二向听，且这对白正是连续安全牌；头名面对庄家立直选择不碰、继续全弃合理。
- 座位 0 序列 101 再立直后，序列 108 的 `1p` 是两名立直者和座位 2 的共同现物。
- 序列 116 的 `8p` 刚由座位 2 打出，两名立直者均已放过；对两家立直者之后持续安全，对座位 2 则是本人现物。
- 序列 124 的赤 `5m` 是两名立直者的共同现物，不能因为四枚 `5m` 已计入可见范围而称其对所有人绝对安全。座位 2 已碰发、存在有役副露威胁；此时手中的 `3z` 已由四枚 `3z` 与四枚公开 `1p` 共同排除普通等待及国士，覆盖三家更完整，严格排序可先切 `3z`。实战优先防两名立直者可以理解，只是一处很小的多家覆盖精度问题。
- 序列 132 再切 `3z` 安全。序列 140 的 `5z` 对座位 1 是现物，对座位 0 是立直后已经放过的牌，对座位 2 是本人现物，正确。

最终流局未听并支付 1500 点，是正确防守的成本，不是过早弃和或漏和。

### 东二一本场：序列 36 是本场最大的纯牌效错误

序列 4 摸 `8p` 后切自风西 `3z`，与切孤张 `4s/7s` 都约为二向听 16 枚；纯枚数相同，但 `3z` 有自风价值，倾向先切孤张数牌。这是小价值损失。

序列 12 摸到第二张 `4s` 后又切一张，仍为二向听约 16 枚；切孤张 `7s` 可约 18 枚，也是一处小错。

序列 36 的 14 张手牌为：

```text
44m 788m 467789p 44z 6z
```

实战摸到第二张 `8m` 后又切一枚 `8m`，打后为二向听约 19 枚；切孤张发 `6z` 同为二向听，却约有 30 枚有效牌，少了约 11 枚。无人立直，也没有防守理由。这是明确大错，核心不是某个局部搭子口诀，而是没有在舍牌前比较完整复合形与孤张。

序列 76 摸 `8m` 后切 `9p`，约为二向听 26 枚；切一张 `7p` 可约 30 枚，属于中等损失。序列 84 摸 `9m` 后切 `8m`，进入一向听约 9 枚，正确。随后他家荣和，未轮到 Zackson 继续。

### 东三：死字牌漏读；副露以后必须只数真正有役的和牌张

#### 序列 22 至 66：第四张 `4z` 被留了太久

序列 10 摸 `5s` 时，`1z` 与 `7z` 都是役牌种子，`2p` 只是孤张；纯牌效相近时应先切 `2p`，实战切场风东 `1z` 是小价值损失。

到序列 22：庄家座位 2、Zackson、座位 0 已各切一枚 `4z`，手中摸到的是第四枚。它对自己的牌效已经完全死亡；实战却切活的 `7z`，约为 48 枚，切 `4z` 可约 51 枚。

之后序列 30、38、46、54 仍持续保留这张死 `4z`：序列 38 切 `2s` 约 11 枚，直接切 `4z` 约 15 枚。直到序列 66 才处理。单次差距不一定巨大，但连续多个节点都没有更新公开枚数，是稳定性问题。

#### 序列 73：吃牌可辩护，但实际只有两枚合法和牌

座位 1 已碰 `1m`，随后又碰三张宝牌 `9s`，是高价值现实威胁，尽管当时尚不能仅凭公开牌确认具体役。

Zackson 吃入座位 2 的 `6p`，固定 `567p`，切 `4s` 后暗手为：

```text
056789m 05567s
副露：567p
名义听 4m / 7m
```

两侧必须分开：

- 和 `7m`：万子可拆为 `567m＋789m`，与 `567p、567s` 组成开放三色；有役，另有赤牌 2。自己持一枚 `7m`、座位 0 河中一枚，当时最多剩 2 枚。
- 和 `4m`：万子只能拆为 `456m＋789m`，三色消失，又因 `9m` 不能断幺；赤牌不是役，因此这侧无役，不能和。

所以该吃是“闭门一向听变成仅 2 枚有役和牌的 3 番听牌”，不是通常意义的 6 枚两面。以 31500 点一位面对明示宝牌 3 的对手，不吃可保留门清与防守弹性；实战吃牌则用高方差换取立即听牌，属于边界选择，不判明确错误。

#### 序列 82、90：实战是有役有效牌的正确改良

序列 82 摸普通 `5m` 后切 `9m`，转为：

```text
5m×2、5s×1、8s×1，共4枚合法和牌
```

三种完成形都有开放断幺九，并有赤牌 2。它把原来只有 `7m×2` 的合法等待改善为 4 枚，不能按“名义 `4m/7m` 有 6 枚”误判为变窄。

序列 90 再摸 `6m`、切 `8m` 后，转为有役的 `4m/7m` 开放三色听牌，约 `4m×4、7m×2`，共 6 枚。`8m` 对座位 1 并不安全，但自己已经是 3 番听牌，继续进攻可以辩护。座位 1 序列 93 再吃 `123p` 后，满贯级终端路线更明显；序列 98 切其现物 `3z` 并维持听牌，正确。

进入听牌后没有任何一家再打出 `4m/7m`。座位 0 的 `7m` 出现在序列 48，早于 Zackson 听牌，不能倒算为漏和。

### 东四：拆雀头退向听；立直以后弃和完整

#### 序列 24：二向听被打回三向听

当时为：

```text
2s 33p 3s 4m4p4s 5z 6m6p6s 7m7s 9m
```

实战切一枚 `3p`，把对子拆成单张，打后由二向听退至三向听。切 `4m、5z` 或 `9m` 都可保持二向听，约 20 枚有效牌。此时座位 1 虽已碰中，但尚早；`3p` 也不是针对该家的安全牌，不能用防守解释。这是本场另一处明确大错。

序列 56 时，手中的 `5z` 已是第三枚计入可见范围的孤张，牌效很弱。实战切 `4m` 约有 46 枚改良；切 `5z` 约 51 枚。座位 1 此时已经碰中、吃 `789p` 两副露：

- 若继续做牌，应切 `5z`；
- 若以一位率优先准备下车，手中 `2s` 是座位 1 现物；
- 实战切非现物 `4m`，既不是最宽进攻，也不是最稳防守。

自己是庄家、对方公开最低仅 1 番，因此序列 56、64 的推进仍属边界判断，不定为明确防守错误；但以当时一位、低价值三向听至二向听的条件，提前用 `2s` 下车会更稳。

座位 0 序列 66 立直后，弃和执行正确：

- 序列 73 `5z`：立直者现物；
- 序列 81 `1z`：立直者刚打，也是座位 1 现物；
- 序列 89 `6m`：立直宣言牌现物；
- 序列 97 `6s`：立直者早巡现物。

最后他家之间荣和，Zackson 点数不变。不能因退向听批评这些安全牌。

### 南一：立直正确；前面仍有两处牌效损失

序列 22 摸 `3z` 时，手中 `2z` 是场风南，仍有役牌价值；`3z` 对 Zackson 不是役牌，且座位 0 已切过一枚。实战切 `2z`，应先切 `3z`。两者不改变当时向听数，是小价值错误。

序列 46 的手牌中，`6s8s` 是完整坎张，而 `2m` 更孤立。实战切 `8s` 后为二向听约 20 枚；切 `2m` 可约 24 枚，是中等牌效损失。

序列 78 切一张 `3s` 后为一向听约 9 枚；切孤张 `6s` 可约 11 枚，是小损失。下一巡实际摸 `4p` 后，实战路线仍成功进入好形听牌，这不能倒推前一手最宽。

序列 86 切 `6s` 立直后为：

```text
345m 233445p 34s 33z
听 2s / 5s
```

`3z` 在南一是西，Zackson 为北家，不是役牌雀头，因此平和成立。按当时可见牌约剩 `2s×1、5s×3`，共 4 枚；默听只有平和低值，立直争取打点合理。最终自摸 `2s`，成立立直、门前清自摸、平和、里宝牌 2，共 5 番满贯；座位 2 支付后被飞，Zackson 以 39500 点逆转一位。

## 放铳、过早弃和与漏和检查

### 放铳

Zackson 本场 **0 次放铳**。三次荣和分别是：

- 东二一本场：座位 1 切 `4m`，座位 0 荣和；
- 东三：座位 2 切 `2z`，座位 1 荣和；
- 东四：座位 2 切 `3m`，座位 0 荣和。

这些不能列入 Zackson 的防守错误。

### 是否过早弃和

没有发现过早弃和：

- 东二从二向听对庄家立直下车，随后面对双立直彻底弃和，正确；
- 东四对立直时为低价值二向听且位居一位，完整弃和正确；
- 本场反而是东三吃牌偏激进、东四面对两副露可能稍晚下车。

### 是否漏和

- 没有发现已经具备合法荣和条件却主动放过和牌张的节点。
- 东一、南一立直后，在自摸以前他家均未打出对应等待牌。
- 东三序列 90 以后确为合法 `4m/7m` 听牌，但随后没有出现这两种牌。
- 东四序列 24 等牌效错误明确降低和率，但替代舍牌会改变后续牌形和押引，无法证明“正确打法必和”。

## 当前最需要练习的三件事

1. **每次准备拆对子或复合形前，强制报数：**`打后向听数 → 雀头是谁 → 有效牌枚数`。重点复现东二一本场序列 36、东四序列 24、东一序列 46。
2. **建立字牌公开枚数表：**每张字牌标记“是否有役 / 已见几枚 / 还剩几枚”。摸到第四枚或三枚已见的孤张时立即重新排序，重点复现东一序列 22、东三序列 22 至 66、东四序列 24 至 56。
3. **副露后只数有役和牌张：**先逐张写出完成后的四面子一雀头和确定役，再统计剩余枚数。重点复现东三序列 74 的“名义 `4m/7m`、实际只有 `7m` 可和”，以及序列 82 合法等待由 2 枚改善到 4 枚。

防守暂时不需要大改。本场应保留的固定流程是：出现立直后先按玩家逐一找现物、立直后通过牌与同巡安全，再决定是否接受退向听和不听罚符。
