# Zackson 单局牌谱复盘（2026-08-31）

## 复盘范围

- 对局：`260831-8ee60653-46a4-4ede-a0b5-bf62ea4dbe35`
- 四人南，Zackson 为座位 1。
- 判断以当时可见信息为准；对手终局手牌只用于核对，不用于结果论倒推。
- 向听数和有效牌用于比较牌效率；遇到立直、副露威胁时，另行考虑安全度、打点和顺位。

## 复核更正

- 东三序列 111 的 `1z` 是绝对安全牌：Zackson 手牌中有三张，座位 2 已于序列 40 打出第四张，四枚全部可见，不可能再构成任何人的和牌张。
- 南一序列 86 的 `8p` 也是当巡绝对安全牌：座位 0 刚在序列 84 打出 `8p`，其余两家在 Zackson 序列 86 再打 `8p` 前都没有摸牌或改变手牌。若 `8p` 原本是某家的和牌张，该家放过后会受同巡振听约束；若原本不是和牌张，手牌未改变时仍然不能荣和。因此这一手的 `8p` 对三家均安全。先使用这张只在当巡安全的牌、保留主要威胁家的长期现物 `5m`，防守顺序正确。

## 结论

终局为 **1 位，32900 点，段位分 +63**。Zackson 与座位 2 同为 32900 点，按雀魂的同点顺位规则取得第一。

本局 9 个小局，和牌 3 次、放铳 0 次、立直 1 次、4 个小局有副露。结果很好，但过程并非没有问题：

1. 防守启动比此前牌谱明显更及时，南一正确利用同巡安全牌，南二也连续使用了立直者现物。
2. 本局没有重现“开始弃和后又切危险牌”的旧问题；南一的 `8p` 是当巡绝对安全牌，不应记为进攻或防守错误。
3. 东三和南三各有一次明确的向听数倒退；基础牌效率仍是当前需要持续训练的部分。
4. 南四的 1100 点自摸具有明确顺位价值：相对第一名正好完成 1400 点差，最终同点头跳。这不是“只和小牌”，而是正确地优先速度。

单个半庄不能证明长期放铳率已经下降；它只能确认这局有多次正确的防守执行。

## 点数进程

| 小局 | 结果 | Zackson 点数变化 | 主要判断 |
| --- | --- | ---: | --- |
| 东一 | 他家荣和 | 0 | 碰中取得确定役，可接受 |
| 东二 | 他家荣和 | 0 | 庄家一向听继续做牌，未发现明确错误 |
| 东三 | 流局未听 | -1500 | 出现牌效倒退；现物间的牌形取舍可优化 |
| 东三一本场 | 他家荣和 | 0 | 无关键攻守错误 |
| 东四 | 荣和 `8s`，2 番 40 符 | +3600（含立直棒收支） | 好形立直正确 |
| 南一 | 他家荣和 | 0 | 正确利用同巡安全牌，保留长期现物 |
| 南二 | 他家自摸 | -4000 | 连续现物防守正确；自摸失分不能算放铳错误 |
| 南三 | 荣和 `4p`，4 番 30 符 | +8700（含 1 根立直棒） | 早段有牌效错误，听牌后的对攻正确 |
| 南四 | 自摸 `4m`，1 番 30 符 | +1100 | 顺位条件下的速度选择正确 |

## 需要改进或讨论的节点

### 1. 东三序列 38：拆掉 `89m`，从一向听退到二向听

当时手牌可整理为：

```text
111z 55m 6m8m9m 56p 123s 5s
```

实战切 `8m`。这会拆掉 `89m` 搭子，打完后成为二向听。

切孤张 `5s`，或整理 `6m / 9m`，都能保持一向听；按当时已见牌计算，约有 11 枚直接进张。此处没有对手攻击，切 `8m` 没有安全收益，因此是明确的纯牌效率错误。

本局东三后面还有同类的小损失：序列 62 切 `3s` 虽仍是一向听，但约 15 枚进张；切一张 `5m` 可保留约 22 枚。问题不是不会做牌，而是复合形和多余牌的比较仍不稳定。

### 2. 东三序列 103：同为现物时的牌形取舍

座位 3 已经立直。序列 95 切 `5s` 是正确防守：对方此前打过赤 `5s`，赤五与普通五属于同一牌种，所以普通 `5s` 也是现物。

下一次出牌前，手牌为：

```text
111z 55m 67m 566p 123s 7s
```

对方河里已经同时有 `1s` 和 `7s`，两张都是现物。实战切 `1s`，拆掉完成的 `123s`，打完后由可维持的一向听退到二向听；切孤张 `7s` 则同样绝对安全，并能保持一向听。

`7s` 是宝牌，这是保留它的价值依据；如果已经决定彻底弃和，切 `1s` 也不能定性为安全错误，因为两张牌对立直者同样安全。若希望保留复攻或流局听牌的可能，切 `7s` 更能维持牌形。这里属于安全度相同后的价值与牌形取舍，严重程度明显低于序列 38 的纯牌效倒退。

序列 111 切东 `1z` 不属于安全牌错误。Zackson 当时持有三张 `1z`，座位 2 又已在序列 40 打出第四张，因此四张东全部可见；手里的三张东从第一张开始就是对所有人的绝对安全牌，序列 111、119、127 连续切东均可用于弃和。此前把它误判为“三张壁下仍可能命中单骑或双碰”，是漏算了牌河中的第四张东。

### 3. 南三序列 30：为了保留单张自风，倒退一个向听数

已经碰白后，手牌为：

```text
副露：555z（白）
手内：1m3m 77m 3p5p 4s0s6s7s 4z
```

实战切 `3m`，拆掉 `13m` 坎张，使打完后的手牌从可维持的一向听退到二向听。切单张自风北 `4z` 可以保持一向听。

北虽然有潜在役牌价值，但当时手牌已经有白、赤 `5s` 和宝牌 `6s`，无需再为一张孤立役牌牺牲整整一个向听数。后来和牌不能证明这一步正确。

## 做得正确的关键判断

### 南一：先使用当巡安全牌，保留长期现物

座位 2 已经碰白、碰中并吃 `123s`，公开最低为白 1 番、中 1 番、宝牌 `2s` 1 番，共 3 番；三副露应按高概率听牌处理。序列 78 切其现物 `6m`，正确进入弃和。

随后座位 0 在序列 84 打出 `8p`，Zackson 于序列 85 摸牌、序列 86 跟打 `8p`，其余两家在此期间都没有摸牌或改变手牌。对任一家而言：若 `8p` 原本能荣和，放过序列 84 的 `8p` 后会在自己的下一次摸牌前处于同巡振听；若原本不能荣和，手牌未改变时序列 86 仍不能荣和。因此 `8p` 在这一手对三家都保证安全。不能仅凭隐藏手牌断言对手“实际进入了振听”，但当时的安全结论成立。

此时手里另有主要威胁座位 2 的长期现物 `5m`。正确的防守顺序正是先打只在当前这一巡保证安全的 `8p`，把之后仍能使用的 `5m` 留作下一层防线。座位 2 在序列 87 摸牌后，`8p` 的同巡安全保证随即失效；Zackson 后来在序列 94 打出所保留的 `5m`，手顺完整。向听数由二向听退到三向听不构成问题，因为此处目标已经是彻底弃和，而不是保持进攻效率。

### 东四：晚巡两面立直

立直切 `7m` 后为：

```text
678m 406p 99p 444s 67s
```

等待 `5s / 8s`，按当时已见牌约剩 7 枚；手牌含赤 `5p`。当时没有对手攻击，默听又没有确定役，因此立直是标准选择。最终荣和 `8s`，手牌本体 2600 点，并收回场上的立直棒。

### 南二：接受自摸失分，不用无筋牌硬顶

座位 3 以 `6m` 立直后，Zackson 先切其现物 `1m`，下一巡再切其现物 `8s`。座位 2 随后也立直，但 Zackson 尚未再次出牌，座位 3 即自摸。

作为庄家损失 4000 点是对手自摸造成的正常失分；这两次出牌都不应因为结果而改判。相较此前多次“二向听顶立直”，这里的防守执行正确。

### 南三：高价值听牌对攻合理

序列 45 吃进 `678s` 后，切北形成坎 `4p` 听牌。手牌有白、两张宝牌 `6s` 和赤 `5s`，共 4 番，约为 7700 点级别。座位 2 随后以庄家立直施压；自己已经高价值听牌，继续切牌维持听牌有充分收益依据。

最终荣和 `4p` 得 7700 点，并取得对方的 1000 点立直棒。这里与南一不同：南一是低价值二向听对三副露高打点威胁，南三则是已经听牌且自身价值足够。

### 南四：碰白进入两面听牌，速度优先正确

南四开始时点数为：

```text
座位0  4100
Zackson 31800
座位2 33200
庄家座位3 30900
```

Zackson 距第一名 1400 点，且只领先庄家 900 点。碰白后切一张 `5m`，形成 `4m / 7m` 两面听牌并获得确定役。

最终 1 番 30 符自摸，自己增加 1100 点，同时第一名支付 300 点；双方相对变化正好是 1400 点，终局同为 32900 点，Zackson 按同点规则取得第一。此处没有必要为了更高打点保持门清，优先把和牌速度最大化是正确选择。

## 与此前牌谱的对照

已确认的正面变化：

- 东三先用 `5s` 现物并以四枚全见的东继续弃和；南一会先消耗当巡安全的 `8p`、保留长期现物 `5m`；南二连续使用 `1m / 8s` 现物。三处都说明防守执行比此前更完整。
- 本局零放铳，且没有出现低价值听牌硬顶庄家可见满贯的情况。
- 南三能区分“高价值听牌可对攻”，南四能按顺位条件选择最低打点的快速和牌。

仍需继续观察或改进的问题：

- 单个半庄不能证明此前的弃和连续性问题已经长期解决，但这局本身没有出现该错误。
- 东三、南三表明无威胁时仍会无理由倒退向听数，这是本局最清楚的实战问题。
- 东三序列 103 表明找到多张现物后，还可以继续比较它们对手牌结构的破坏程度。

## 接下来 10 个半庄的训练要求

1. 无攻击信号时，每次出牌前确认一句：`这张打完是几向听？` 若向听数倒退，必须能说出安全、打点或顺位上的明确补偿。
2. 牌效率练习继续以“五组理论、完成面子不轻拆、复合形有效牌比较”为主；本局重点重放东三序列 38 和南三序列 30。
3. 单独记录两个牌效指标，目标都为 0：
   - 无威胁时无理由倒退向听数；
   - 同向听选择之间存在明显有效牌差，却选择较窄的一项。
4. 防守方面继续保持本局南一的顺序：先消耗即将失效的同巡安全牌，再保留长期现物供后续使用。
5. 继续记录“开始弃和后又切危险牌”的次数，用于观察旧问题是否稳定消失；本局该项为 0。

重新核对后，本局最明确的问题是无威胁时的基础牌效率，而不是南一的防守。长期仍应继续监控弃和连续性，但不能再把南一序列 86 或东三序列 111 当作负面案例。
